spring.application.name=backend

# Server Configuration
server.port=8080

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/doctor_appointment_db
spring.datasource.username=root
spring.datasource.password=9014090842@Sai

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration - MUST be at least 32 characters
jwt.secret=mySuperSecretKeyThatIsVeryLongAndSecure1234567890abcdef
jwt.expiration=86400000

# CORS - This is just for reference, actual config is in WebSecurityConfig
cors.allowed.origins=http://localhost:5174,http://localhost:5173